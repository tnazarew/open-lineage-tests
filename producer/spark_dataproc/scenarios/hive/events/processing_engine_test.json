{
  "eventType": "COMPLETE",
  "job": {
    "namespace": "default",
    "name": "simple_test.execute_insert_into_hive_table.warehouse_t2"
  },
  "run": {
    "facets": {
      "processing_engine": {
        "version": "{{ any(result) }}",
        "name": "spark",
        "openlineageAdapterVersion": "{{ any(result) }}"
      }
    }
  }
}